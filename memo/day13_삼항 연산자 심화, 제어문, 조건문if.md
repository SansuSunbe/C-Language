
- 제어문 
	1) 조건문
		```
		- if문
			 if(조건식){ -> 거짓이면 else if로 감
				 실행할 문장;
				 }
			 else if(조건식){ -> 위의 조건식이 false면 실행
				 실행할 문장;
				 } 
			 else{ -> 위의 모든 조건식이 false일 때 실행
				실행할 문장;
				 }
		``` 